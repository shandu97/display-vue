const state = {
    
}
const getters = {
    text(state) {
      return state.text
    },
    msg(state) {
      return state.msg
    },
    btn(state) {
      return state.btn
    },
}

export default {
    state,
    getters
}